//Given two dates, your function should return which one comes before the other.

// const minDate = (...rest) => rest.reduce((min, curr) => Math.min(min, curr));

// minDate("02/05/2021", "24/01/2021");
